kubernetes-dashboard:
  protocolHttp: true

  extraArgs:
    - --enable-skip-login
    - --enable-insecure-login
    - --system-banner="Welcome to Kubernetes"

  ingress:
    enabled: true
  
  hosts:
    - kubernetes-dashboard.local

  settings:
    clusterName: witcherek7-k8s-cluster